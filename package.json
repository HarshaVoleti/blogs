{
  "name": "harsha-blog",
  "version": "1.0.0",
  "description": "A VitePress blog",
  "scripts": {
    "setup": "bash deploy.sh",
    "docs:dev": "vitepress dev docs",
    "docs:build": "bash deploy.sh && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
